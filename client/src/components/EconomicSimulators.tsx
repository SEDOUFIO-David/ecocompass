/**
 * Design: Atlas académique vivant — simulations volontairement simples, explicables et clairement distinctes de données réelles.
 */
import { useMemo, useState } from "react";
import { Area, AreaChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { BarChart3, Calculator, SlidersHorizontal } from "lucide-react";

function Slider({ label, value, min, max, step = 1, suffix = "", onChange }: { label: string; value: number; min: number; max: number; step?: number; suffix?: string; onChange: (value: number) => void }) {
  return <label className="sim-slider"><span>{label}<b>{value}{suffix}</b></span><input type="range" value={value} min={min} max={max} step={step} onChange={(event) => onChange(Number(event.target.value))} /></label>;
}

export default function EconomicSimulators() {
  const [active, setActive] = useState<"market" | "inflation" | "budget">("market");
  const [supplyShift, setSupplyShift] = useState(0);
  const [demandShift, setDemandShift] = useState(0);
  const [basketInflation, setBasketInflation] = useState(8);
  const [income, setIncome] = useState(150000);
  const [expenses, setExpenses] = useState(112000);
  const marketData = useMemo(() => Array.from({ length: 10 }, (_, index) => { const price = index + 1; return { price, Offre: Math.max(0, 14 + price * 9 + supplyShift), Demande: Math.max(0, 112 - price * 8 + demandShift) }; }), [supplyShift, demandShift]);
  const equilibrium = marketData.reduce((closest, item) => Math.abs(item.Offre - item.Demande) < Math.abs(closest.Offre - closest.Demande) ? item : closest, marketData[0]);
  const inflationData = [{ label: "Avant", panier: 100 }, { label: "Après", panier: Number((100 * (1 + basketInflation / 100)).toFixed(1)) }];
  const purchasingPower = Number((100 / (1 + basketInflation / 100)).toFixed(1));
  const savings = income - expenses;
  return <section className="simulators"><div className="simulators-heading"><div><p className="eyebrow">Essayer un raisonnement</p><h2>Trois simulations pour manipuler une idée, pas pour prédire le monde réel.</h2></div><p>Les paramètres et résultats sont volontairement simplifiés. Ils servent à visualiser un lien possible entre des variables.</p></div><div className="sim-tabs"><button type="button" className={active === "market" ? "active" : ""} onClick={() => setActive("market")}><SlidersHorizontal size={16} /> Offre et demande</button><button type="button" className={active === "inflation" ? "active" : ""} onClick={() => setActive("inflation")}><BarChart3 size={16} /> Inflation</button><button type="button" className={active === "budget" ? "active" : ""} onClick={() => setActive("budget")}><Calculator size={16} /> Budget</button></div>{active === "market" && <div className="sim-panel"><div className="sim-controls"><p className="eyebrow">Hypothèse pédagogique</p><h3>Déplacez les conditions d’offre et de demande.</h3><Slider label="Variation de l’offre" value={supplyShift} min={-25} max={25} suffix=" unités" onChange={setSupplyShift} /><Slider label="Variation de la demande" value={demandShift} min={-25} max={25} suffix=" unités" onChange={setDemandShift} /><div className="sim-readout"><span>Point de rencontre approximatif</span><b>Prix {equilibrium.price} · {Math.round((equilibrium.Offre + equilibrium.Demande) / 2)} unités</b><p>Ce point n’est qu’une lecture visuelle de ce modèle très simplifié.</p></div></div><div className="sim-chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={marketData} margin={{ top: 18, right: 10, left: -18, bottom: 0 }}><CartesianGrid vertical={false} stroke="#DCE8E0" strokeDasharray="3 3" /><XAxis dataKey="price" tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} label={{ value: "Prix (repère illustratif)", position: "insideBottom", offset: -2, fill: "#637772", fontSize: 11 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><Tooltip contentStyle={{ border: "1px solid #DCE8E0", borderRadius: 12, background: "#FFFDF8" }} /><Line type="monotone" dataKey="Offre" stroke="#0E6356" strokeWidth={3} dot={false} /><Line type="monotone" dataKey="Demande" stroke="#C17C36" strokeWidth={3} dot={false} /></LineChart></ResponsiveContainer></div></div>}{active === "inflation" && <div className="sim-panel"><div className="sim-controls"><p className="eyebrow">Panier illustratif</p><h3>Voyez l’effet d’une hausse générale des prix sur un panier fixe.</h3><Slider label="Hausse du panier" value={basketInflation} min={0} max={30} suffix=" %" onChange={setBasketInflation} /><div className="sim-readout"><span>Repère de pouvoir d’achat</span><b>{purchasingPower} / 100</b><p>Si le revenu reste identique, un panier plus coûteux réduit ce que le même budget permet d’acheter.</p></div></div><div className="sim-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={inflationData} margin={{ top: 18, right: 10, left: -18, bottom: 0 }}><defs><linearGradient id="basketGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#C17C36" stopOpacity=".42" /><stop offset="100%" stopColor="#C17C36" stopOpacity="0" /></linearGradient></defs><CartesianGrid vertical={false} stroke="#E9DFCF" strokeDasharray="3 3" /><XAxis dataKey="label" tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 12 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><Tooltip contentStyle={{ border: "1px solid #E2D4BD", borderRadius: 12, background: "#FFFDF8" }} /><Area type="monotone" dataKey="panier" name="Valeur du panier (indice)" stroke="#C17C36" strokeWidth={3} fill="url(#basketGradient)" /></AreaChart></ResponsiveContainer></div></div>}{active === "budget" && <div className="sim-panel"><div className="sim-controls"><p className="eyebrow">Budget illustratif</p><h3>Reliez revenus, dépenses et épargne restante.</h3><Slider label="Revenu mensuel" value={income} min={50000} max={350000} step={5000} suffix=" FCFA" onChange={setIncome} /><Slider label="Dépenses mensuelles" value={expenses} min={20000} max={350000} step={5000} suffix=" FCFA" onChange={setExpenses} /><div className={`sim-readout ${savings < 0 ? "sim-readout-alert" : ""}`}><span>Épargne illustrée</span><b>{savings.toLocaleString("fr-FR")} FCFA</b><p>{savings < 0 ? "Dans cet exemple, les dépenses dépassent le revenu : il faut explorer les postes et les arbitrages." : "Dans cet exemple, une partie du revenu reste disponible après les dépenses renseignées."}</p></div></div><div className="sim-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={[{ label: "Revenu", value: income }, { label: "Dépenses", value: expenses }, { label: "Épargne", value: Math.max(0, savings) }]} margin={{ top: 18, right: 10, left: -18, bottom: 0 }}><CartesianGrid vertical={false} stroke="#DCE8E0" strokeDasharray="3 3" /><XAxis dataKey="label" tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 12 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><Tooltip formatter={(value) => `${Number(value).toLocaleString("fr-FR")} FCFA`} contentStyle={{ border: "1px solid #DCE8E0", borderRadius: 12, background: "#FFFDF8" }} /><Area type="monotone" dataKey="value" name="Montant" stroke="#0E6356" strokeWidth={3} fill="#B9DCC7" fillOpacity={.45} /></AreaChart></ResponsiveContainer></div></div>}<p className="sim-disclaimer">Simulation pédagogique — valeurs fictives et relations simplifiées. Elle ne constitue ni une prévision, ni un conseil financier, ni une mesure économique réelle.</p></section>;
}
