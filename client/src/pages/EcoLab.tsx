/**
 * Design: Atlas académique vivant — EcoLab guide l’observation et rend les limites des données visibles avant toute interprétation.
 */
import { useMemo, useState } from "react";
import { Area, AreaChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { ArrowRight, BarChart3, Compass, FlaskConical, Lightbulb, Scale, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import SiteShell from "@/components/SiteShell";
import PageHero from "@/components/PageHero";
import AtlasTrail from "@/components/AtlasTrail";
import { demoSeries, labQuestions, type DemoCountry, type DemoIndicator } from "@/data/ecolab";

export default function EcoLab() {
  const [country, setCountry] = useState<DemoCountry>("Togo");
  const [comparison, setComparison] = useState<DemoCountry>("Ghana");
  const [indicator, setIndicator] = useState<DemoIndicator>("Activité (indice)");
  const [start, setStart] = useState(2018);
  const [response, setResponse] = useState<number | null>(null);
  const series = demoSeries.find((item) => item.label === indicator) || demoSeries[0];
  const data = series.values[country].filter((point) => point.year >= start);
  const compareData = useMemo(() => series.values[country].map((point, index) => ({ year: point.year, [country]: point.value, [comparison]: series.values[comparison][index].value })), [country, comparison, series]);
  const change = data.length > 1 ? data[data.length - 1].value - data[0].value : 0;
  return <SiteShell>
    <PageHero variant="page-hero-lab" eyebrow="EcoLab · pratique économique" title={<>Lire une donnée,<br /><em>commencer une analyse.</em></>} description="Sélectionnez une série de démonstration, observez-la puis formulez une première lecture. EcoLab entraîne à distinguer ce que le graphique montre, ce qu’il ne montre pas et ce qu’il faudrait vérifier." aside={<div className="lab-hero-note"><FlaskConical size={22} /><b>Données de démonstration</b><span>Aucune valeur affichée ici ne constitue une statistique officielle ni une prévision.</span></div>} />
    <AtlasTrail label="Trajectoire d’une lecture de données" tone="ochre" steps={[{ label: "Source", note: "statut visible" }, { label: "Série", note: "unité & période" }, { label: "Observation", note: "décrire" }, { label: "Question", note: "interpréter" }, { label: "Limite", note: "à vérifier" }]} />
    <section className="bg-[#F8F5ED] py-10 lg:py-14"><div className="container"><div className="lab-control-bar"><label>Pays<select value={country} onChange={(event) => { setCountry(event.target.value as DemoCountry); setResponse(null); }}><option>Togo</option><option>Ghana</option></select></label><label>Indicateur<select value={indicator} onChange={(event) => { setIndicator(event.target.value as DemoIndicator); setResponse(null); }}>{demoSeries.map((item) => <option value={item.label} key={item.label}>{item.label}</option>)}</select></label><label>Période à partir de<select value={start} onChange={(event) => setStart(Number(event.target.value))}>{[2018, 2019, 2020, 2021].map((year) => <option value={year} key={year}>{year}</option>)}</select></label><div className="lab-source"><span>STATUT</span><b>Démonstration locale</b><small>{series.unit} · {series.source}</small></div></div><div className="lab-explorer-grid"><article className="lab-chart-card"><div className="lab-card-title"><div><p className="eyebrow">Explorateur de données</p><h2>{indicator} · {country}</h2></div><span>{data[0].year}–{data[data.length - 1].year}</span></div><div className="lab-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{ top: 20, right: 12, left: -17, bottom: 0 }}><defs><linearGradient id="labArea" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#0E6356" stopOpacity=".38" /><stop offset="100%" stopColor="#0E6356" stopOpacity="0" /></linearGradient></defs><CartesianGrid vertical={false} stroke="#DCE8E0" strokeDasharray="3 3" /><XAxis dataKey="year" tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><Tooltip contentStyle={{ border: "1px solid #DCE8E0", borderRadius: 12, background: "#FFFDF8" }} /><Area type="monotone" dataKey="value" name={indicator} stroke="#0E6356" strokeWidth={3} fill="url(#labArea)" /></AreaChart></ResponsiveContainer></div><div className="lab-meta"><span>Variation illustrative sur la période</span><b className={change >= 0 ? "lab-up" : "lab-down"}>{change >= 0 ? "+" : ""}{change} points d’indice</b><p>{series.note}</p></div></article><aside className="lab-reading"><p className="eyebrow">Que remarquez-vous ?</p><h2>Commencez par décrire avant d’expliquer.</h2><div>{labQuestions.map((question, index) => <button type="button" className={response === index ? "lab-question-active" : ""} onClick={() => setResponse(index)} key={question}><span>0{index + 1}</span>{question}</button>)}</div>{response !== null && <div className="lab-feedback"><Lightbulb size={17} /><p>{response === 0 ? "Décrivez la direction de la série, puis indiquez les années qui semblent rompre cette tendance. Évitez de parler de cause à ce stade." : response === 1 ? "Repérez une variation plus marquée et transformez-la en question : qu’aimeriez-vous connaître sur la période, le territoire ou la méthode ?" : "Avant toute explication, vérifiez la définition, l’unité, la source, la période et ce que la série ne mesure pas."}</p></div>}</aside></div></div></section>
    <section className="bg-white py-12 lg:py-16"><div className="container"><div className="section-heading"><div><p className="eyebrow">Comparer sans classer</p><h2>Deux séries, une lecture prudente.</h2></div><p>Cette comparaison n’indique pas qu’un pays est « meilleur ». Elle invite à formuler des questions sur les définitions, les contextes et les périodes.</p></div><div className="lab-compare-controls"><label>Comparer avec<select value={comparison} onChange={(event) => setComparison(event.target.value as DemoCountry)}><option>Togo</option><option>Ghana</option></select></label><span><Scale size={16} /> Même indicateur · mêmes années · démonstration</span></div><div className="lab-compare-chart"><ResponsiveContainer width="100%" height="100%"><LineChart data={compareData} margin={{ top: 20, right: 12, left: -17, bottom: 0 }}><CartesianGrid vertical={false} stroke="#DFE8E1" strokeDasharray="3 3" /><XAxis dataKey="year" tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><YAxis tickLine={false} axisLine={false} tick={{ fill: "#637772", fontSize: 11 }} /><Tooltip contentStyle={{ border: "1px solid #DCE8E0", borderRadius: 12, background: "#FFFDF8" }} /><Legend wrapperStyle={{ fontSize: 12 }} /><Line type="monotone" dataKey={country} stroke="#0E6356" strokeWidth={3} dot={false} /><Line type="monotone" dataKey={comparison} stroke="#C17C36" strokeWidth={3} dot={false} /></LineChart></ResponsiveContainer></div></div></section>
    <section className="bg-[#123139] py-12 text-white"><div className="container lab-next"><div><p className="eyebrow text-[#9FD5B9]">Passer à la pratique</p><h2>Une série ouvre une question. Une simulation aide à tester un mécanisme.</h2></div><div><Link href="/economie-reelle" className="cta-light"><BarChart3 size={18} /> Ouvrir les simulations <ArrowRight size={17} /></Link><div className="lab-disclaimer"><ShieldAlert size={16} /> EcoLab propose des données fictives, des scénarios simplifiés et des pistes de lecture — jamais des prédictions.</div></div></div></section>
  </SiteShell>;
}
